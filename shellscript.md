```
#!/usr/bin/bash  -> 어떤쉘로 스크립트를 실행할지 정의하는 곳
echo $(which bash)

쉘에는 sh(초기의 유닉스 쉘),ksh,csh(c언어기반쉘),bash(sh와 대부분호환)
```
```
- 변수는 대,소문자 구분한다.
- 변수의 이름은 숫자로 시작할 수 없다.
- 변수에는 모든 값을 문자열로 저장된다.
- 변수에는 자료형을 기입하지 않는다.(자료형에 상관없이 넣을 수있다)
- 

```
